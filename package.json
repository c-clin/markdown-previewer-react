{
  "name": "markdown-previewer-react",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "npm run build && mv build/index.html build/200.html && surge build markdown-previewer-react.surge.sh/"    
  },
  "devDependencies": {
    "marked": "^0.3.19"
  }
}
